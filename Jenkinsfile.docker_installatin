pipeline {
    agent any

    stages {

        stage('Docker Test') 
        {
            steps {
                sh '''
                whoami
                docker --version
                docker ps
                '''
            }
        }

        
    }
}